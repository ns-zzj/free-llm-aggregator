# 待办 / 已知小问题

（按发现时间倒序。修完就把条目挪到下面「已修」或者在提交信息里说清，别让它烂在这儿。）

## 恢复可用：提供商那个改成批量文案 + 模型上加一个（2026-09-15，用户定的，先不做）

用户原话：「那把提供商的搞成只要有一个不能用就显示"全部恢复可用"，模型那里再加个按钮。不过先记着待办，先不干。」

背景：状态本来就是模型级的（`source_state` 主键 `provider_id + model_id`），但提供商行上那个「恢复可用」
其实调的是 `POST /providers/:id/state` → `setStatusForProvider()`，**把该来源下所有模型一起清成 available**。
入口长在提供商行上、文案又只写「恢复可用」，容易以为是单个来源的范围；而且想手动恢复**某一个**模型时，
现在只剩下面两条路，都不理想：自动等冷却探测（要等 + 不一定过），或者模型行的「测试」（按真实调用处理，占速率额度、花钱）。

要做的两件（都在后台界面）：

1. **提供商行**（`public/app.js` 的 `providerRow()`）：
   - 显示条件从「聚合状态 ≠ available」改成 **只要该来源下有一个模型不是 available 就显示**
     （即 `modelsAvailable < modelsTotal`；后端 `/overview` 已经把 `modelsTotal`/`modelsAvailable` 算好了，
     见 `src/admin/routes.js` 的 `aggregateHealth()`）
   - 文案改成 **「全部恢复可用」**（点下去就是整家一起清，文案得说清范围）
2. **模型行加一个「恢复可用」按钮**：
   - 前端：模型管理表格（`renderModalModelList()` 里 `table(['对外模型名','上游 model id','图片','状态','操作'])`）
     的「操作」列，`state.status !== 'available'` 时才出现
   - 后端：加 `POST /providers/:id/models/:modelId/state`，内部用**已导出的** `sourceState.setStatus(providerId, modelId, 'available')`
     （`src/store/sourceState.js:158`），十来行，不用新写存储逻辑
   - 语义与「测试」互补：**测试 = 真跑一次、通过才恢复**；**恢复 = 我配置/额度已经修好了，别浪费一次真实请求**

这是逻辑改动，动手时先把 m6 / m8 里碰后台状态接口的用例看一眼。

## 想做，但没定（Gemini 原生格式）

用户 2026-09-15 提的，**只记着，先不做**。原话：「用这个协议的客户端太少了。我的 gemini 上游也没余额。
先加进待办吧。不知道能不能做。」

**卡在哪（用户 2026-09-17 确认）**：不是"写不了"，是**没 key 没法验**。这个项目里每个适配器都是拿真 API 调出来的
（Cloudflare 的三种返回方言、Responses 缺字段被官方 SDK 静默丢事件、Anthropic 的 thinking 签名，全是实测才发现的），
盲写出来的适配器比没有更糟 —— 它看起来能用，等哪天填了 key 才发现对不上，还查不出是哪一层的问题。
**所以真要做的第一件事是拿到一个 key 做真机联调**（Google AI Studio 的免费 key 就行，不绑卡、跟 Vertex 的余额不是一回事），
或者找个愿意帮忙的第三方当真机验证者（**别开口借 key**，改成"我给你命令、你在你机器上跑、把原始响应贴回来"）。
在此之前这条不动。

先记结论，省得以后重新查：

- **上游**：`https://generativelanguage.googleapis.com/v1beta/openai/` 是 Google 官方的 OpenAI 兼容层
  （[文档](https://ai.google.dev/gemini-api/docs/openai)）。按我们现在"baseUrl 当完整前缀 + 只拼端点"的规矩，
  **它今天就能当 `openai-compatible` 用，一行代码都不用加**。所以「想用 Gemini 的额度」根本不缺这个适配器。
- **客户端**：只有 google-genai SDK（能改 baseUrl）和 Gemini CLI（靠社区 PR 才支持自定义地址）发原生格式。
  第三方工具基本不为它单独写一套 —— 这是它和 Anthropic 那份协议最大的区别。

真要做的两种（**是两件不同的事，别混**）：

1. **上游适配器 `gemini`**（便宜，照 `anthropic.js` 那个模板抄，约 300 行 + `test/m13.test.js`）
   - `POST {baseUrl}/models/{model}:generateContent`（非流式）／`:streamGenerateContent?alt=sse`（流式）
   - 鉴权 `x-goog-api-key`（Vertex 那套是 OAuth `Authorization: Bearer`、模型名写成 `publishers/google/models/...`，算另一条线）
   - 请求：`contents[].parts[]`（role 是 `user`/`model`，**必须交替**）、`systemInstruction`；参数全塞进
     `generationConfig`（`maxOutputTokens`/`temperature`/`topP`/`stopSequences`/`responseMimeType`/`responseSchema`）；
     工具是 `tools[].functionDeclarations` + `toolConfig.functionCallingConfig.mode`
   - 响应：`candidates[].content.parts[]` + `finishReason`（`STOP`/`MAX_TOKENS`/`SAFETY`/`RECITATION`/`PROHIBITED_CONTENT`
     要映成 `stop`/`length`/`content_filter`）；用量是 `usageMetadata`
   - 流式：真 SSE 但**没有事件名、也没有 `[DONE]`**，靠 `finishReason` 收尾
   - 模型名在**路径**里（和 `cloudflare-workers-ai` 一样），所以 `realModelId` 不进请求体
   - ⚠ 思考：多轮工具调用里 `parts[].thoughtSignature` 必须原样回传，丢了会被 400 / 降级
     （[HelixML 那篇](https://helixml.tech/blog/adding-support-for-gemini-thought-signatures)、
     [plexus 的修补](https://github.com/mcowger/plexus/commit/bf29c338123546131c6f8f86b379247fa0ec9024)）；
     思考预算走 `generationConfig.thinkingConfig.thinkingBudget`，和我们现在的 `reasoning_effort` 对不上。
     建议照现有惯例：**明确不做思考**，在 README 已知限制里写一条（跟 Anthropic 面那条一个写法）。
   - 图片只有 `inlineData`（base64），没有"图片 URL"这回事 —— 要么自己下回来转 base64，要么明确拒
2. **下游方言 `gemini`**（贵得多：等于第四种客户端协议 + 一套 `faces/` 实现 + 流式事件 + 后台地址栏）
   - 只有「客户端是 Gemini CLI / google-genai SDK」时才需要，和上面第 1 条是两件独立的事

## 2.1.0（原计划叫 2.0.1，用户 2026-09-17 改成 2.1.0）

> 改号理由：探测超时从 30 秒改成 120 秒并**新增了一个后台设置项**，属于加功能；再加上 baseUrl 拼接规则变了（行为变化），
> 两件加起来够 minor，所以走 2.1.0。

**已完成**（2026-09-17）：

- ✅ 探测/测试超时改成后台可配：`probe.js` 的 `PROBE_TIMEOUT_MS = 30 * 1000` → `probeTimeoutMs()`（读设置、默认 120 秒、范围 5~600）；
  `settings.js` 加 `probe_timeout_seconds: '120'`；`admin/routes.js` 加进 PATCH 白名单 + 校验；`public/app.js` 设置页加了那一格。
  测试：m5 14/14、m6 12/12、m8 9/9、m10 7/7 全过。
  ⚠ m1m2 的 **M2-4 / M2-8 是早就存在的脆弱用例**（stash 掉改动在原始代码上跑一样失败），跟这次改动无关，别再被它误导。
  起因：NVIDIA NIM 走前缀代理实测 **17~46 秒**才出响应头，30 秒探测必然误判成"超时"，而真实请求走 120 秒 ——
  症状是"客户端能用、后台测试说超时"。
- ✅ `docs/设计文档.md` 里那行假的 `proxyUrl | 可选出网代理` 已删（代码里早删了，见 `src/store/providers.js:55` 和 `:128`）；
  数据库表那节的列清单也按实际代码更正了。
- ✅ `baseUrl` 拼接规则的说明已写进 `docs/Release-v2.1.0.md`：以前 anthropic / openai-responses 填裸域名也能跑
  （会自动补 `/v1`，已用 2.0.0 的源码核实过），2.1.0 起必须自己带上；后台那行「实际请求：…」用来核对。
  **已发出的 2.0.0 Release 不动。**
- ✅ 版本号已升 `package.json` + `package-lock.json` → 2.1.0，`docs/Release-v2.1.0.md` 已写
  （代码注释/文档里那几十处 `2.0.0` 是历史记录，不改）。

**还差**：

- ⬜ 在**有 Docker 的机器**上打镜像：`npm run image`（产物 `dist/nszzj-free-llm-aggregator_2.1.0_x86-64.tar.gz`）；
  这台开发机没装 Docker，打不了。
- ⬜ 推 GitHub + 发 Release v2.1.0（正文用 `docs/Release-v2.1.0.md`），服务器上重新 `git pull` + 打镜像/起容器。

## 写文档的约定（用户 2026-09-15 定的，免得反复改 README）

- **README 只写"长期成立"的东西**：功能、环境变量、安装方式、真正的设计取舍与限制。
- **一次性的、下个版本就会变的**（"现在只有 x86-64，我明天用 QEMU 构建个 arm64"这种）
  写在 **Release 正文**里 —— 那里改起来不碰源码，也不会让 README 天天变。
- 判断标准很简单：**这句话半年后还成立吗？** 不成立就别进 README。

---

## ✅ 已做（2.0.0，2026-09-15）· 上游适配器 `openai-responses`

> **已完成**，实现方式比当初设想的更进一步：除了上游适配器，**下游也加了 Responses 方言**
> （`/openai/responses`），翻译逻辑统一放在 `src/protocol/responsesMapping.js`（两个方向一处实现）。
> 下面这段原始记录留档，说明当时的判断依据。测试见 `test/m10.test.js`（上游）与 `test/m12.test.js`（下游）。

**背景**：OpenAI 2025-03 起主推 [Responses API](https://developers.openai.com/api/docs/guides/migrate-to-responses)
（`POST /v1/responses`），定位是 Chat Completions 的继任者；OpenAI 明说 Chat Completions
「会继续长期支持」，但要下线的是老的 **Assistants API**（2026 上半年）。
新模型（gpt-5.x 那代）在 Responses 上能力更全：推理项、更好的工具调用、内建工具
（web_search / file_search / code_interpreter / image_generation / 远程 MCP）、服务端会话状态。

**已经定下的做法（用户 2026-09-15）**：**不做下游兼容层**，改成**在上游适配器里加一个协议选项**
—— 也就是和 `openai-compatible` / `anthropic` / `cloudflare-workers-ai` 并列的
`openai-responses`。客户端面继续只提供 `/v1/chat/completions`，谁的上游说 Responses
就给它挂这个适配器，翻译在这一层做。

**边界（先写清楚，免得以后误解）**：这个适配器只解决「**上游**说 Responses」。
**只认 Responses 的客户端**（新版 OpenAI SDK 默认、Codex CLI、Agents SDK 那类）打到我们这儿
仍然是 404（走 `src/app.js` 里那条"未实现该端点"的兜底），要另做一层下游的才算支持 —— 那是另一件事。

**要做的事（翻译清单）**：
- 请求：`messages` → `input`（items）+ `instructions`（system 提出来）；
  `max_tokens` → `max_output_tokens`；`response_format` → `text.format`；没有 `n`
- 响应：`output`（items）→ `choices[].message`；`output_text` → `content`；
  `usage` 形状不同（input/output tokens 加明细）
- 流式：**命名事件**（`response.output_text.delta` / `response.completed` …）→ 现有的 OpenAI chunk 形状，
  收尾照样发 `[DONE]`
- 工具调用：Responses 的 schema 更扁、**默认 strict**、调用结果是 `function_call` / `function_call_output`
  两种独立 item；要翻成 chat completions 的 `tool_calls` / `role: tool`（两边对应关系见官方「function calling」指南）
- **一定带 `store: false`**：默认会在上游留 30 天会话状态，我们这边是无状态的聚合网关，别替用户留痕迹
- 参考：上面那条迁移指南；差异的通俗版 <https://dev.to/dev-in-progress/chat-completions-vs-openai-responses-api-what-actually-changed-4bco>；
  Simon Willison 的解读 <https://simonwillison.net/2025/Mar/11/responses-vs-chat-completions>

**像谁**：工作量比 `anthropic` 适配器小一点（形状更像 OpenAI 自己），但流式事件和工具 item 是新的。

---

## ✅ 已修（2026-09-15）· 首次设置页：自动生成的下游 apikey 被说成"沿用原来的"

> **已修**：`/setup` 现在回三值 `accessKeyAction`（`changed` / `kept` / `generated`），
> 前端按值给文案 —— 留空且库里本来没有时会明说「已自动生成一条」。
> 旧的 `accessKeyChanged` 保留（兼容别的调用方）。下面留档原始记录。
>
> ⚠ 教训：修完之后要在 `docs/待办.md` 里标掉 —— 用户有一次按"文档说修了"去查代码，
> 发现其实没修（那条一直挂着"待修"）。

**现象**：全新部署第一次进 `/admin`，首次设置页的「下游 apikey」留空提交 → 后端自动生成了一条，
但弹窗写的是「下游 apikey **沿用原来的**：`sk-agg-…`」。库里本来一条都没有，"沿用"是错的。

**严重程度：纯文案，key 没丢。**
返回的就是新生成那条的明文（`result.accessKey`），还自动复制进剪贴板了 ——
用户拿得到、能直接用。问题只是措辞会让人怀疑"我是不是没设置成功／它偷偷保留了别人的 key"。

**位置**
- `public/app.js` 首次设置页那段 —— `const kept = !result.accessKeyChanged;` 之后按这个布尔二选一给文案
- `src/admin/routes.js` 的 `/setup` —— `accessKeyChanged` 只在"用户自己填了口令"时为 `true`；
  留空时，无论"沿用旧口令"还是"新建一条"，都是 `false`

**根因**：后端只回一个布尔，把两件不同的事混成了同一个 `false`：

1. 留空 + 库里已有口令 → 沿用（真的沿用了）
2. 留空 + 库里没有 → 新生成一条（跟"沿用"没有任何关系）

**修法（建议）**：把那个布尔换成三值 `accessKeyAction`，前端按值给文案，一次说清：

| 值 | 什么情况 | 文案 |
|---|---|---|
| `changed` | 用户填了口令 | 新的下游请求 apikey：… |
| `kept` | 留空 + 原本就有 | 沿用原来的：… |
| `generated` | 留空 + 原本没有 | 已自动生成一条：… |

`else` 分支里看 `primary()` 是不是 `null` 就能区分，不用多查一次库。
`accessKeyChanged` 建议保留（别的调用方可能在看），只是前端改用新字段。

**顺带看一眼的两处（同一段代码，先没动）**
- 首次设置页：`keeps` 为真（已有口令）时，用户点了「更改」但没填内容 →
  前端会自己 `randomAccessKey()` 塞一条发过去（而不是留空）。行为上等于"点了更改就换一条"，
  可以接受，但没有二次确认文案。
- 那句「沿用原来的（`sk-****`）—— 这条是早期版本建的，看不到明文」
  只在密文解不开（`APP_SECRET` 换过）时才会走到，正常情况碰不到。
